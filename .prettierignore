# Django templates are not plain HTML: prettier has no Django parser, so it
# reflows {% ... %} tags onto shared lines and makes the templates hard to
# read. They are formatted by hand instead.
templates/

# Generated by local test runs.
.pytest_cache/
